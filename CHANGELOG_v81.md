# AutoSync Clean v81

## Sửa lỗi

- Sửa lỗi danh sách tự cuộn lên trên sau khi tích hoặc bỏ tích checkbox ở một hàng.
- Checkbox giờ chỉ cập nhật trạng thái chọn và vùng trạng thái của hàng tương ứng, không kích hoạt rebuild toàn bộ danh sách sau chu kỳ refresh 3 giây.
- Giữ nguyên vị trí cuộn hiện tại khi người dùng đang xem các cửa sổ ở phía dưới danh sách.

## Giữ nguyên từ v80

- Chuột phải vào một dòng Offline để xóa trực tiếp mà không cần tích checkbox.
- Giữ cơ chế đồng bộ chuột/phím, chế độ nhẹ máy, FPS sync và preview như trước.
